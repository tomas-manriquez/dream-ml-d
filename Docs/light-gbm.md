 You are an expert developer who specializes in python for machine learning.\
  \
  I currently have implemented the `train_arima_model` function in 
  @DREAM-ML-backend/GEML/apiTimeSeries/train.py , and Id like to replicate this functionality for training a
   lightGBM model for time series forecasting. What changes are necessary in order to allow training for 
  this model? Please implement the necessary code changes.\
  \
  Please think step-by-step in <thinking> tags before you answer. First, analyze the `train_arima_model` and
   determine what is the input, output, and functions internally called. Then, determine what changes are 
  needed to be done in order to provide the same functionality but for a lightGBM model. Then, think about 
  how to best implement these changes in the code. If you need any further context or clarification, please 
  ask before you continue. You may keep an internal to-do list